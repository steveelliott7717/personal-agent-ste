[supervisord]
nodaemon=true
logfile=/dev/stdout
logfile_maxbytes=0        ; disable rotation for supervisord’s own log
pidfile=/tmp/supervisord.pid

[program:export-watcher]
command=/app/bin/run_email_watcher.sh
directory=/app
autorestart=true
startretries=999

; send program logs to stdout/stderr with rotation disabled
stdout_logfile=/dev/stdout
stdout_logfile_maxbytes=0
stderr_logfile=/dev/stderr
stderr_logfile_maxbytes=0
